section: screens
   hercules:
   zeus: 
end

section: links
  hercules:
    right = zeus
  zeus:
    left = hercules
end

section: options
  switchDoubleTap = 1500
  switchDelay = 10000
  heartbeat = 500
  relativeMouseMoves = true # Good for video games and screen locking
  keystroke(alt+f1) = ; lockCursorToScreen(toggle)
  screenSaverSync = true
end
